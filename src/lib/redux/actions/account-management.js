import * as TYPES from "../types";
import axios from "axios";

export const apiUrl = "https://abcstudyonline.herokuapp.com";

export const ApiChangePassword = (accessToken,currentPassword,newPassword) => (dispatch,getStore)=>{
    return ;
}